# Browser utils.ConfigLoader (edge,chrome,firefox)
# Fallback value if no Maven argument provided
browser=edge

# Enable or disable headless mode (true or false)
# Fallback value if no Maven argument provided
headless=false

# Enable or disable screenshot capture on test failure (true or false)
# Fallback value if no Maven argument provided
capture_screenshot=true

# Directory to save screenshots
# Fallback value if no Maven argument provided
screenshot_directory=src/test/reports/screenshots/

# Enable or disable custom HTML repo -rt generation (true or false)
# Fallback value if no Maven argument provided
generate_html_report=true

# Directory to save custom HTML reports
# Fallback value if no Maven argument provided
report_directory=src/test/reports/

# Test Environment URL
# Fallback value if no Maven argument provided
test.env.url=https://thinking-tester-contact-list.herokuapp.com/

# Data Source utils.ConfigLoader
# Available options: csv, mysql, postgresql
# Fallback value if no Maven argument provided
data.source=mysql

# CSV File Path (only if data.source=csv)
# Fallback value if no Maven argument provided
csv.file.path=src/test/resources/TS3_create_new_contact/CNC_009_AddMultipleContactsCreateContacts_data.csv

# Database utils.ConfigLoader
# These will only be used if the data source is set to mysql or postgresql
# Fallback value if no Maven argument provided
db.name=test_data
db.table=contact_data_2023_08_26T12_05_37
db.user=mysql
db.host=localhost
db.port=3306